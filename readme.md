# Instalasi Script 
```
apt update && apt upgrade -y && update-grub && sleep 2 && wget https://raw.githubusercontent.com/DhedyKurnia/Premium/main/sc.sh && chmod +x sc.sh && ./sc.sh
```

```
sysctl -w net.ipv6.conf.all.disable_ipv6=1 && sysctl -w net.ipv6.conf.default.disable_ipv6=1 && apt update && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/DhedyKurnia/XrayCol/main/vlu4a.sh && chmod +x vlu4a.sh && ./vlu4a.sh
```
